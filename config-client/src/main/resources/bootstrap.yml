spring:
  cloud:
    config:
      discovery:
        serviceId: config-server
        enabled: true
      profile: dev
      label: master
    bus:
      trace:
        enabled: true
  application:
    name: config-client
  rabbitmq:
    host: 192.168.99.103
    password: dawn
    port: 5672
    username: dawn
server:
  port: 8881
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false
